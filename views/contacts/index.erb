<h1>Contact List</h1>
<ul>
<% @contacts.each do |contact| %>
  <li>
    <h2><a href="/contacts/<%= contact.id %>"><%= contact.first_name %></a>
    <span><%= contact.importance %></span>
    </h2>
  </li>
<% end %>
</ul> 